export type User = {
  _id: string;
  username: string;
  email: string;
  createdAt: string;
  nickname: string;
  updatedAt: string;
  token: string;
};

export type UserMessage = {
  _id: string;
  username: string;
  nickname: string;
};
