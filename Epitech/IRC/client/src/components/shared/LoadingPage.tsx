export default function LoadingPage() {
  return (
    <div className="fixed inset-0 flex items-center justify-center bg-opacity-50 bg-background">
      <div className="w-12 h-12 bg-blue-500 rounded-full animate-pulse" />
    </div>
  );
}
